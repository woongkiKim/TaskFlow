# ──────────────────────────────────────────────
# TaskFlow 환경변수 템플릿
# 이 파일을 복사해서 .env 로 만든 뒤 실제 값을 채워주세요.
# cp .env.example .env
# ──────────────────────────────────────────────

# Mock 모드 (true = 백엔드 없이 더미 데이터로 실행)
VITE_USE_MOCK=false

# Django 백엔드 API 주소
VITE_API_BASE_URL=http://localhost:8000/api
